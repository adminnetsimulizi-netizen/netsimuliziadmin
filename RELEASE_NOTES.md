# Release Strategy

Development -> Internal QA -> Closed/Beta testing -> Production.

Do not publish the app until payment callbacks, wallet accounting, authentication,
offline entitlement checks, content protection and recovery flows have been tested.
