NET SIMULIZI — FINAL WEBSITE UI
All HTML pages are bundled in this ZIP.
Frontend is intentionally self-contained (CSS/JS embedded per page) for IIS testing.
Flow: land -> home -> story -> chapters -> reader.
Account, author and admin pages are included.
PHP/API and MariaDB integration comes next and will be shared with the App.
